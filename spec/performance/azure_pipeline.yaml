pool:
  vmImage: 'ubuntu-latest'

steps:
- script: |
    docker run k6 run /spec/performance/Stages_low_vus.js
  displayName: Run k6 from Docker locally

##schedules:
##  - cron: '0 0 * * *'
##    displayName: Daily midnight build
##    branches:
##      include:
##        - main