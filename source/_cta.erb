<% if data.cta_type == "Start" %>
  <a id="<%= data.id %>" href="<%= data.slug + ".html" %>" role="button" draggable="false" className="govuk-button govuk-button--start" data-module="govuk-button">
    <%= data.title %>
    <svg className="govuk-button__start-icon" xmlns="http://www.w3.org/2000/svg" width="17.5" height="19"
         viewBox="0 0 33 40" aria-hidden="true" focusable="false">
      <path fill="currentColor" d="M0 0h13l20 20-20 20H0l20-20z"/>
    </svg>
  </a>
<% else %>
  <% classMap = {
    "Start" => "",
    "Primary" => "",
    "Secondary" => "govuk-button--secondary",
    "Warning" => "govuk-button--warning"
  }
     c = data.cta_type
     selected_class = classMap[c]
  %>

  <a id="<%= data.id %>"
     href="<%= data.slug + ".html" %>"
     class="<%= "govuk-button " + selected_class %>"
     data-module="govuk-button">
    <%= data.title %>
  </a>
<% end %>