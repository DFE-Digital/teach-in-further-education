<% breakpoints = get_breakpoints %>
<div id="<%= data.id %>" class="app-advice <%= data.apply_background_color ? "shaded" : "" %>" role="region" aria-label="<%= data.title %>">
  <% if !data.media.nil? %>
    <% if data.display_order %>
      <div class="app-advice-with-image-1">
        <div class="app-image-1">
          <%= partial "image", :locals => { :data => data.media, :breakpoints => breakpoints}%>
        </div>
        <div>
          <% if data.show_title %>
            <h2 class="govuk-heading-l"><%= data.title %></h2>
          <% end %>
          <% if !data.advice_body.nil? %>
            <%= markdown(data.advice_body) %>
          <% end %>
        </div>
      </div>
    <% else %>
      <div class="app-advice-with-image-2">
        <div>
          <% if data.show_title %>
            <h2 class="govuk-heading-l"><%= data.title %></h2>
          <% end %>
          <% if !data.advice_body.nil? %>
            <%= markdown(data.advice_body) %>
          <% end %>
        </div>
        <div class="app-image-2">
          <%= partial "image", :locals => { :data => data.media, :breakpoints => breakpoints } %>
        </div>
      </div>
    <% end %>
  <% else %>
    <% if data.show_title %>
      <h2 class="govuk-heading-l"><%= data.title %></h2>
    <% end %>
    <div>
      <% if !data.advice_body.nil? %>
        <%= markdown(data.advice_body) %>
      <% end %>
    </div>
  <% end %>
</div>