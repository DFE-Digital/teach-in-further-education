import React from 'react'

const Button = (props) => {
    return (
        <button class="govuk-button" data-module="govuk-button">
            Save and continue
        </button>
    )
}

export default Button;